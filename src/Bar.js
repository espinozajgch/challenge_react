import {Toolbar, AppBar} from '@material-ui/core';

function Bar() {
    
    return (
        <AppBar position="static">
        <Toolbar>
            
        </Toolbar>
        </AppBar>
            );
  }

  export default Bar;